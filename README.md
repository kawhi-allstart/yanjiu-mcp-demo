# yanjiu-mcp-demo

Static webpage demo created in Codex CLI with MCP showcase.

## MCP quick use

- Use branch-first workflow instead of writing directly to `main`.
- Update files with SHA validation to avoid accidental overwrite.
- Create PR and inspect changed files before merge.
